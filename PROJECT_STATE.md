# PROJECT STATE — Halfway Lock

Last updated: 2026-09-07 — release-preparation pass complete on branch `hoplite/phleious-16c2ad27` (draft PR #1).

## What this is
An original, data-driven narrative decision game built in Godot 4.7.2 (GDScript).
The player is the Keeper of Summit Lock No. 9 ("Halfway") on the Meridian Canal.
Every card is a vessel, petitioner, order or emergency demanding one of two decisions.
Exactly 1,000 playable decision cards ship (see `content/CARD_INVENTORY.json`).

## Verified status (fresh runs; details in the linked reports)
| Item | Status |
|---|---|
| Cards | **1000 / 1000** — onboarding 30, evergreen 220, resources 150, relationships 120, minor_arcs 180, major_arcs 180, crises 70, endings_meta 50 |
| Characters / arcs / endings | 25 records (20 characters + 5 sources); 12 major (15 cards) + 30 minor (6 cards); 40 endings, 3 true routes, every ending has a data trigger |
| Content validation | 0 errors, 0 warnings (`reports/validation/content_validation.json`) |
| Dialogue analysis | 0 errors, 562 warnings (length guidelines; `docs/CONTENT_REPORT.md`) |
| Arc audit | 42 arcs, 0 problems (`reports/content/arc_audit.json`) |
| Engine tests | **250 checks, 0 failures**, twice from clean import (`reports/validation/engine_tests.json`) |
| End-to-end playtest (UI level) | **47 checks, 0 failures** (`reports/validation/end_to_end_playtest.md`) |
| Save/load | Float→int coercion fix, RNG continuation, v1→v2 migration, corrupt recovery, full-state round-trip |
| Simulation | 10,000 runs: 0 stalls, 1 fallback run, median 50 watches, 25 endings reached, 17 true endings, 10/12 major arcs completed (`docs/BALANCE_REPORT.md`) |
| Exports | Linux (smoke-tested), Windows (unsigned, built), Web (builds and loads; interactive test blocked in sandbox); Android preset only |
| Assets | 125 art + 30 audio files, all original procedural candidates; manifests validate (0 problems); licence register empty by design |
| Legal docs | `PRIVACY.md`; `CREDITS.md` and `THIRD_PARTY_NOTICES.md` generated by `tools/build_credits.py` |
| Human approval needed | All visual and audio assets; device testing; legal review; signing (`docs/RELEASE_CHECKLIST.md`) |

## Environment facts (verified in sandbox)
- Godot 4.7.2 stable is **not** preinstalled; `.hoplite/settings.json` setup installs it plus
  Linux/Windows/Web export templates.
- Python 3.12 with `jsonschema`, `pillow`, `numpy`; `ffmpeg` present (music transcoding).
- No image or audio generation service is available; candidates are procedural.
- Headless Chrome in the sandbox has no WebGL 2, so the Web export cannot be played there.

## Key commands
- Import resources (first run): `godot --headless --import --path .`
- Run the game: `godot --path .`
- Engine tests: `godot --headless --path . game/tests/TestRunner.tscn`
- UI playtest: `godot --headless --path . game/tests/Playtest.tscn`
- Smoke play: `godot --headless --path . game/tests/Smoke.tscn` (or `<export> --headless -- --smoke`)
- Validate content: `python3 tools/validate_content.py && python3 tools/analyze_dialogue.py && python3 tools/audit_arcs.py`
- Validate assets: `python3 tools/validate_assets.py`
- Simulate: `python3 tools/simulate_runs.py --runs 10000 --out reports/simulations/simulation.json`
- Reports/localization: `python3 tools/generate_reports.py --all`
- Credits/notices: `python3 tools/build_credits.py`
- Exports: see `docs/BUILD_AND_EXPORT.md`

## How to resume
1. Read `docs/REMAINING_WORK.md` for open items and `docs/RELEASE_CHECKLIST.md` for go/no-go.
2. Read `docs/DECISION_LOG.md` for why things are the way they are.
3. Run the key commands above before changing anything.
